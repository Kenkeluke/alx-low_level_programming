#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * Description : This program that prints its name
 * followed by a new line
 *@argc: command line argument count
 *@argv: array of command line string arguments
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
if (argc >= 0)
{
printf("%s\n", argv[0]);

}

return (0);
}
